const Header = () => {
    const title = 'Cocktail App'

    return (
        <div>
            <article>
                <h1>{title}</h1>
            </article>
        </div>
    );
};

export default Header;
