const Header = () => {
    const title = 'Hello World'

    return (
        <div>
            <article>
                <h1>{title}</h1>
            </article>
        </div>
    );
};

export default Header;
